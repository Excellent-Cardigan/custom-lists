# Copy to .env to enable the AI curation path (optional).
# Without this, the app uses the offline mock curator and still runs end-to-end.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# ANTHROPIC_MODEL=claude-sonnet-5
# PROXY_PORT=8787
